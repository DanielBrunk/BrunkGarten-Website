---
import Layout from '../components/Layout.astro';
import Hero from '../components/Hero.astro';
import Section from '../components/Section.astro';
import Card from '../components/Card.astro';
import { siteConfig } from '../config';
---

<Layout>
  <Hero
    tagline="Garten- und Landschaftsbau am Bodensee"
    title="Wir gestalten Ihren <em>Traumgarten</em>"
    subtitle="Professioneller Garten- und Landschaftsbau in Radolfzell und am Bodensee. Von der Planung bis zur Umsetzung — fachgerecht, zuverlässig und mit Leidenschaft für Grün."
    ctaText="Jetzt Angebot anfragen"
    ctaHref="#kontakt"
    ctaSecondaryText="Leistungen entdecken"
    ctaSecondaryHref="#leistungen"
    stats={[
      { value: '100%', label: 'Leidenschaft' },
      { value: 'Regional', label: 'Am Bodensee' },
      { value: 'Komplett', label: 'Aus einer Hand' },
    ]}
  />

  <!-- Leistungen -->
  <Section title="Unsere Leistungen" id="leistungen" background="alt">
    <div class="grid">
      <Card title="Gartengestaltung" icon="🌿">
        <p>Individuelle Gartenplanung und -gestaltung. Von der Neuanlage bis zur Umgestaltung — wir schaffen grüne Wohlfühlorte.</p>
      </Card>
      <Card title="Pflasterarbeiten" icon="🧱">
        <p>Terrassen, Wege, Einfahrten und Mauern. Fachgerecht verlegt mit hochwertigen Materialien für dauerhafte Ergebnisse.</p>
      </Card>
      <Card title="Bepflanzung" icon="🌱">
        <p>Professionelle Bepflanzungskonzepte mit standortgerechter Pflanzenauswahl. Staudenbeete, Hecken, Bäume und Sträucher.</p>
      </Card>
      <Card title="Erdarbeiten" icon="⛏️">
        <p>Geländemodellierung, Aushub und Bodenaustausch. Wir schaffen die Basis für Ihr Projekt — termingerecht und sauber.</p>
      </Card>
      <Card title="Zaunbau" icon="🪵">
        <p>Sichtschutz, Holzzäune, Metallzäune und Gabionen. Sicher, langlebig und passend zum Gesamtbild Ihres Gartens.</p>
      </Card>
      <Card title="Bewässerung" icon="💧">
        <p>Automatische Bewässerungssysteme für Rasen, Beete und Topfpflanzen. Wassersparend und komfortabel.</p>
      </Card>
    </div>
  </Section>

  <!-- Über uns -->
  <Section title="Über uns" id="ueber-uns">
    <div class="about">
      <div class="about__text">
        <p>
          <strong>{siteConfig.companyName}</strong> ist Ihr Partner für professionellen Garten-
          und Landschaftsbau in {siteConfig.address.city} und am Bodensee. Als inhabergeführter
          Betrieb legen wir besonderen Wert auf handwerkliche Qualität, nachhaltige Materialien
          und persönliche Beratung.
        </p>
        <p>
          Von der ersten Idee bis zum fertigen Garten begleiten wir Sie durch jede Phase
          Ihres Projekts. Ob private Gartengestaltung oder gewerbliche Außenanlage —
          wir sind Ihr zuverlässiger Partner vor Ort.
        </p>
        <ul>
          <li>Inhabergeführter GaLaBau-Betrieb</li>
          <li>Persönliche Beratung und Planung vor Ort</li>
          <li>Termintreue und saubere Ausführung</li>
          <li>Faire und transparente Preise</li>
        </ul>
      </div>
    </div>
  </Section>

  <!-- Kontakt -->
  <Section title="Kontakt" id="kontakt" background="alt">
    <div class="contact">
      <div class="contact__info">
        <p>
          Sie haben ein Projekt oder eine Frage? Rufen Sie uns an oder schreiben Sie uns —
          wir melden uns schnellstmöglich bei Ihnen.
        </p>
        <div class="contact__details">
          <p>
            <strong>Telefon</strong><br />
            <a href={`tel:${siteConfig.phone}`}>{siteConfig.phoneDisplay}</a>
          </p>
          <p>
            <strong>E-Mail</strong><br />
            <a href={`mailto:${siteConfig.email}`}>{siteConfig.email}</a>
          </p>
          <p>
            <strong>Adresse</strong><br />
            {siteConfig.address.street}<br />
            {siteConfig.address.zip} {siteConfig.address.city}
          </p>
        </div>
        <div class="contact__hours">
          <strong>Öffnungszeiten</strong>
          {siteConfig.openingHours.map((entry) => (
            <p>{entry.days}: {entry.hours}</p>
          ))}
        </div>
      </div>
    </div>
  </Section>
</Layout>

<style>
  .grid {
    display: grid;
    gap: var(--space-xl);
  }

  .about__text {
    max-width: var(--max-width-narrow);
    margin-inline: auto;
  }

  .about__text p + p {
    margin-top: var(--space-md);
  }

  .about__text ul {
    margin-top: var(--space-lg);
  }

  .contact {
    max-width: var(--max-width-narrow);
    margin-inline: auto;
  }

  .contact__details {
    margin-top: var(--space-xl);
    display: grid;
    gap: var(--space-lg);
  }

  .contact__hours {
    margin-top: var(--space-xl);
  }

  .contact__hours p {
    margin-top: var(--space-xs);
  }

  @media (min-width: 768px) {
    .grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .contact__details {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }
</style>
